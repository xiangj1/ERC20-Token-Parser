from html.parser import HTMLParser

class MyHTMLParser(HTMLParser):

    def handle_starttag(self, tag, attrs):

        if tag == "a":

           for name, value in attrs:

               if name == "href":
                   print(value)
                   #print (value.split("/")[2])


parser = MyHTMLParser()


info_page = open('t.html', 'r').read()
parser.feed(info_page)

#https://etherscan.io/tokens
